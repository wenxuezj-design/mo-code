{
  "name": "mo-code",
  "private": true,
  "type": "module",
  "scripts": {
    "cli": "node --no-warnings --experimental-strip-types src/cli.ts",
    "test": "node --experimental-strip-types --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.0"
  },
  "dependencies": {
    "glob": "^13.0.6"
  }
}
