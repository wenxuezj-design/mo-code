{
  "name": "mo-code",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cli": "node --env-file-if-exists=.env --no-warnings --experimental-strip-types src/cli/main.ts",
    "test": "node --experimental-strip-types --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.113.0",
    "glob": "^13.0.6"
  }
}
