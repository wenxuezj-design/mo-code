---
status: accepted
---

# 授权记忆遵循 allow 的裁决优先级

会话授权和持久授权属于用户已经确认的预先允许，在权限裁决中位于显式 `ask` 之后、权限模式的默认行为之前。显式 `deny`、`plan` 对编辑和非只读 Shell 的硬拒绝，以及显式 `ask` 都不能被授权记忆绕过；`dontAsk` 则允许已有授权继续执行。

这个顺序让当前进程中的授权记忆与重新启动后加载的 `permissions.allow` 规则保持相同行为，避免同一持久授权在写入前后产生不同结果。
