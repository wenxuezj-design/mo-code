---
status: accepted
---

# WebFetch 持久授权按网络来源匹配

用户允许一次 `web_fetch` 并选择不再询问时，mo-code 按 URL 的协议、主机和端口创建持久授权，同一来源下的其他路径可以复用该授权，但不同协议、子域名或端口仍需重新确认。

Claude Code 的 WebFetch 规则按域名匹配；本项目选择更严格的网络来源，是为了在减少重复 URL 确认的同时，不把一次 HTTPS 授权自动扩展到 HTTP、其他子域名或其他端口。该授权写入本地项目配置，并在确认界面中展示最终允许的来源。
