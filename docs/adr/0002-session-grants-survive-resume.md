---
status: accepted
---

# 会话授权随原会话恢复

会话授权绑定到 Agent Session，而不是启动 mo-code 的单次进程。使用 `--resume` 恢复同一个 Session 时继续沿用已有授权；新建 Session 或从原 Session 分支时不继承，以免把针对原对话上下文作出的允许决定扩散到新的执行上下文。

因此，会话授权需要作为 Session 状态持久化。这个选择增加了少量会话存储工作，但让“会话授权”的含义与可恢复会话保持一致，也避免进程重启意外改变权限行为。
