# Agent 权限

本上下文定义 mo-code 在执行 Agent 工具调用时使用的权限与授权术语，避免把授权持续时间和授权对象混为一谈。

## Language

**授权有效期**：
一次允许决定保持有效的时间跨度，分为单次、会话和持久三种。
_Avoid_：授权范围

**单次授权**：
只适用于当前一次工具调用的允许决定。

**会话授权**：
绑定到同一个 Agent 会话的允许决定；恢复该会话时继续有效，新建或分支出的会话不继承。

**持久授权**：
当前交互会话结束后，仍可用于后续匹配工具调用的允许决定。

**精确命令授权**：
只匹配与用户已确认内容完全相同的 Shell 命令，不自动推断命令前缀或通配规则。
_Avoid_：命令族授权、前缀授权

**本地项目配置**：
只适用于当前项目和当前开发者、不用于团队共享的项目配置；它仍属于项目侧配置，采用其中的权限扩张内容需要项目信任。
_Avoid_：本地配置

**网络来源授权**：
按 URL 的协议、主机和端口匹配的网络访问授权；同一来源下的不同路径共享授权。
_Avoid_：域名授权、URL 授权

**显式询问规则**：
要求每次匹配的工具调用都重新获得用户确认的权限规则，不能被会话授权或持久授权跳过。
_Avoid_：可记忆询问

**权限扩张配置**：
能够减少后续工具确认或放宽默认执行能力的项目侧权限配置；当前包括允许规则和默认权限模式。
_Avoid_：自动授权配置、安全配置

**拒绝反馈**：
用户拒绝工具调用时提供给 Agent 的可选说明，用于帮助 Agent 调整后续方案；它不会授权或执行被拒绝的调用。
_Avoid_：拒绝原因

**编辑会话授权**：
允许同一 Agent 会话中的普通文件编辑不再重复询问的会话授权；显式权限规则和受保护资源仍然可以收紧它。
_Avoid_：临时 acceptEdits、全局编辑授权

**授权建议**：
工具调用在需要确认时可以提供的授权有效期和匹配目标；它描述用户选择“不再询问”后可以记住什么，本身不是允许决定。
_Avoid_：授权决定、自动授权

**授权降级**：
记忆授权失败时，把用户已经确认的当前工具调用作为单次授权继续执行，并明确告知授权没有被记住。
_Avoid_：静默失败、持久授权成功

**工具拒绝**：
阻止当前工具调用但继续当前 Agent 轮次的用户决定，模型会收到相应的失败结果。
_Avoid_：中断

**轮次中断**：
停止当前 Agent 轮次以及尚未处理的工具和确认请求的用户操作，不等同于拒绝某个工具调用。
_Avoid_：工具拒绝

**授权记忆**：
由用户确认后保留的会话授权或持久授权，在权限裁决中等同于预先允许，但不能绕过显式询问、拒绝或只读模式约束。
_Avoid_：权限缓存、自动放行

**主工作目录**：
启动 Agent 时的当前工作目录及其子目录，是默认无需额外目录授权的文件访问范围；Git 根目录不会自动扩大这一范围。
_Avoid_：项目根目录、仓库根目录、工作区根目录

**外部路径**：
传入位置或符号链接真实目标位于主工作目录之外、未被默认授权的文件或目录路径；它不是永久禁区，可以通过明确规则或用户确认获得访问许可。
_Avoid_：非法路径、禁止路径

**保护路径**：
传入位置或符号链接真实目标承载仓库元数据或 Agent 控制信息、修改时需要比普通文件更严格裁决的路径；普通允许规则和编辑会话授权不能自动跳过该保护，读取仍遵循普通读取规则。
_Avoid_：敏感路径、禁止路径

**灾难删除熔断**：
即使启用 `bypassPermissions`，递归删除文件系统根目录或当前用户 Home 目录仍必须单独确认的不可记忆安全边界。
_Avoid_：删除保护、普通 Shell 确认

**路径权限门禁**：
工具执行前根据文件访问目标判断主工作目录、外部路径和保护路径的应用层权限边界；它不对工具进程施加操作系统级隔离。
_Avoid_：文件系统沙箱、路径沙箱

**文件系统访问目标**：
一次工具调用准备读取、写入或删除的具体路径集合，独立于权限规则用于匹配工具调用的目标字符串。
_Avoid_：权限目标、permissionTarget

**项目信任**：
用户允许 mo-code 采用某个项目提供的权限扩张配置；它不表示项目内容天然安全，也不影响项目提供的限制性权限规则生效。
_Avoid_：项目安全、信任所有项目内容、工作区信任

**信任根**：
项目信任所绑定的规范目录身份，也是项目配置向父目录查找时不能越过的边界；Git 项目使用最近的仓库根目录，非 Git 项目使用 Agent 启动目录，且不因此扩大主工作目录的文件访问范围。
_Avoid_：主工作目录、配置根目录

**信任记录**：
保存在用户目录中、表示某个信任根已被用户接受的持久状态；项目自身不能提供、修改或覆盖该状态。
_Avoid_：项目配置、权限规则、信任缓存

**会话项目信任**：
只在当前 mo-code 进程中有效、不会生成信任记录的项目信任；当前用于直接从用户 Home 目录启动的场景。
_Avoid_：会话授权、临时工具授权

**信任提示**：
交互模式首次进入尚无信任记录的信任根时，在 Agent 构造和项目权限扩张配置生效之前显示的项目级确认。
_Avoid_：工具权限提示、首次工具确认

**受限项目会话**：
当前项目未获得信任但仍继续运行的会话；它既可由用户暂不信任产生，也可由非交互运行缺少信任记录产生。项目提供的权限扩张配置被忽略，用户配置及项目限制性规则仍然生效。
_Avoid_：只读模式、权限拒绝、退出项目
